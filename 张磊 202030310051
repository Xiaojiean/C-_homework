const限定符；const的变量不是一个常量表达式，可以在声明和定义中都添加相应的关键词，无论是const*int*p=&i，const int&q=i，其实都是只是对指针或引用的声明。表明不能通过修改*p的值，或q的值来达到修改i的值，i的值能否被修改取决于i属于什么类型，如果为非常量值，则可以被其他变量修改
“using namespace std”使得程序可以在使用std中的命名
当函数的返回类型为void时，不用在函数中使用return，若不提供返回，则可以使用return
预处理器编译指令，预处理器将会在编译之前使用iostrem的内容替换预处理指令
const修饰的为常量不可修改，const*为常量指针，则指针指向的数值不能更换，指向可以更换，*const为指针变量，指向不能修改，指向的数值可以修改。
